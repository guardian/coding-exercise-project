
var assert = require('assert')

var gol = require('../src/game-of-life')

describe('Game of Life', function() {
    describe('a test', function() {
        it('should be failing', function() {
            assert(gol.value, 'Change this to be more useful');
        });
    });
});
